{
  "name": "charmdrop",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "vercel-build": "npm install --prefix web && npm run build --prefix web && rm -rf .vercel && cp -R web/.vercel .vercel"
  }
}
